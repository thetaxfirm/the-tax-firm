# ============================================================
# VERCEL ENVIRONMENT VARIABLES
# Copy each KEY=VALUE pair into Vercel Dashboard:
# Project Settings > Environment Variables
#
# VITE_* values are compiled into the client bundle at BUILD time, so they must
# be present before the first deploy (and a change to one needs a redeploy).
# Everything else is read by the serverless function at request time.
#
# ENV_TEMPLATE.md documents what each variable does.
# ============================================================

# --- REQUIRED: Database ---
DATABASE_URL=<your-mysql-connection-string>
# Example: mysql://user:password@host:port/database?ssl={"rejectUnauthorized":true}

# --- REQUIRED: Authentication (Google OAuth 2.0) ---
# Authorized redirect URI in the Google console must be:
#   https://<your-domain>/api/oauth/callback   (add one per domain, incl. previews)
JWT_SECRET=<long-random-string>                    # openssl rand -hex 32
GOOGLE_CLIENT_ID=<xxxx.apps.googleusercontent.com>
GOOGLE_CLIENT_SECRET=<GOCSPX-...>
VITE_GOOGLE_CLIENT_ID=<same-value-as-GOOGLE_CLIENT_ID>
ADMIN_EMAILS=chris@thetaxfirm.us                   # comma-separated

# --- REQUIRED: Object storage (S3-compatible: Cloudflare R2, AWS S3, B2) ---
S3_BUCKET=<bucket-name>
S3_ACCESS_KEY_ID=<access-key>
S3_SECRET_ACCESS_KEY=<secret-key>
S3_REGION=auto                                     # "auto" for R2
S3_ENDPOINT=<https://ACCOUNTID.r2.cloudflarestorage.com>   # omit for AWS S3
S3_PUBLIC_URL=<https://cdn.thetaxfirm.us>          # durable public object URLs
# S3_FORCE_PATH_STYLE=true                         # only if your provider needs it

# --- REQUIRED: GoHighLevel CRM ---
GHL_API_KEY=<your-ghl-api-key>                     # JWT, no "Bearer " prefix
GHL_LOCATION_ID=hf2fpQyPswcNJOmnqRFR

# --- REQUIRED: Blog API ---
BLOG_API_KEY=<your-blog-api-key>

# --- REQUIRED: App Branding ---
VITE_APP_TITLE=The Tax Firm
VITE_APP_LOGO=<your-logo-cdn-url>

# --- RECOMMENDED: Owner email notifications (Resend) ---
RESEND_API_KEY=<re_...>
NOTIFY_EMAIL_TO=chris@thetaxfirm.us
NOTIFY_EMAIL_FROM=The Tax Firm <notify@thetaxfirm.us>
OWNER_EMAIL=chris@thetaxfirm.us
OWNER_NAME=Christopher Craig

# --- OPTIONAL: Analytics ---
VITE_ANALYTICS_ENDPOINT=<your-analytics-endpoint>
VITE_ANALYTICS_WEBSITE_ID=<your-analytics-website-id>

# --- OPTIONAL: Google Drive Blog Sync (only for the sync worker) ---
DRIVE_FOLDER_ID=<your-google-drive-folder-id>
GOOGLE_API_KEY=<your-google-api-key>

# --- SET AUTOMATICALLY BY VERCEL — do not add manually ---
# NODE_ENV=production
# PORT (not used by serverless functions)

# ============================================================
# LEFT OVER FROM THE MANUS PLATFORM
#
# No code reads these any more. Nothing breaks if they stay set; delete them
# from the Vercel project whenever you want to tidy up:
#   VITE_APP_ID
#   OAUTH_SERVER_URL
#   VITE_OAUTH_PORTAL_URL
#   BUILT_IN_FORGE_API_URL
#   BUILT_IN_FORGE_API_KEY
#   VITE_FRONTEND_FORGE_API_URL
#   VITE_FRONTEND_FORGE_API_KEY
#
# OWNER_OPEN_ID is still read (it grants the admin role to one openId), but a
# value carried over from Manus can never match: openIds are now "google:<sub>".
# Use ADMIN_EMAILS instead, and either update OWNER_OPEN_ID or drop it.
# ============================================================
